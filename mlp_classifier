from sklearn.neural_network import MLPClassifier


mlp = MLPClassifier(hidden_layer_sizes=(3,), activation='logistic', max_iter=2000, random_state=42)

mlp.fit(X, y)

print(f"Training Accuracy: {mlp.score(X, y) * 100:.2f}%")
